import yaml



# def read_yaml():
#     """ A function to read YAML file"""
#     with open('ibm_config.yml') as f:
#         config = yaml.safe_load(f)
 
#     return config

# config = read_yaml()
# print(config[description])


with open('ibm_config.yml') as f:
    docs = yaml.load(f, Loader=yaml.FullLoader)

    # print(docs['herbivore_sp2'])

    print(docs['ibm_display']['delay_between_frames'])

    # for doc in docs:
    #     # for k, v in doc.items():
    #     #     print(k, "->", v)
    #     #     print(k['grid_size'])
    #     print(list(doc.keys()))
    #     print(doc)

    # sorted = yaml.dump(data, sort_keys=True)
    # print(sorted)

    # print(sorted[enable_output])
